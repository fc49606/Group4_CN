apiVersion: apps/v1
kind: Deployment
metadata:
  name: revenue
spec:
  selector:
    matchLabels:
      app: revenue
  template:
    metadata:
      labels:
        app: revenue
    spec:
      containers:
      - name: revenue-container
        image: eu.gcr.io/cn2021-kubernetes-colab/microservice-revenue
      - name: cloud-sql-proxy
        image: gcr.io/cloudsql-docker/gce-proxy:1.17
        command:
          - "/cloud_sql_proxy"
          - "-instances=cn2021-kubernetes-colab:europe-west2:cn-database=tcp:5432"
          - "-credential_file=/secrets/service_account.json"
        securityContext:
          runAsNonRoot: true
        volumeMounts:
        - name: volumekey
          mountPath: /secrets/
          readOnly: true
      volumes:
      - name: volumekey
        secret:
          secretName: sa-secret